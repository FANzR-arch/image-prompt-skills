---
name: halftone-ui-collage
description: "把文章、产品或工作流主题编译成「网点 UI 拼贴横幅」提示词——暖色平涂底上，黑白网点人物带白色贴纸描边、扁平矢量 UI 卡片、底角有机色块与手绘涂鸦同框，左三分之一压超大无衬线标题，5:2 超宽横板。用户提到网点拼贴、halftone 拼贴、编辑拼贴封面、贴纸描边人物、扁平 UI 卡片封面、editorial collage banner、halftone collage，或要给一组文章成套做同骨架换配色的横幅时使用。先选配色寄存器与模块，再编译单条确定性提示词。不用于写实摄影、3D 渲染、纯扁平图标风或产品 UI 截图。"
---

# Halftone UI Collage（网点 UI 拼贴横幅编译器）

把一个主题编译成**一条**可直接生图的提示词。骨架锁死、配色寄存器可换：暖色平涂底上，一个黑白网点人物带白色贴纸描边坐在桌前，右侧浮两张扁平白卡，左三分之一压一块超大黑标题。

核心原则：**三种渲染语言，各守边界。** 网点只落在人物、椅子和笔记本机身；屏幕、卡片、背景、桌面绝对平整干净。任何一方越界，风格立刻塌——网点爬上卡片就变脏拼贴，卡片的干净感蔓延到人物就变廉价矢量插画。

## 画幅

**5:2 超宽横板锁死。** 出图参数 `3200×1280`、quality `high`——网点密度和卡片小字都吃分辨率，低分辨率下网点糊成灰块、卡片小字烂掉。

16:9 可用但需重排四带预算（标题块要让位）。方图不可用，左右四带叙事会被压死。

## 风格 DNA（八项，换任何主题不变）

1. **网点作用域只有三样**：人物、椅子、笔记本机身。屏幕、卡片、桌面、背景、有机形状永远平整。
2. **白色贴纸描边只跟人物轮廓**，不跟笔记本、桌子、椅子、卡片——描边爬到家具上是这套风格最刺眼的失手。
3. **五角色色板，一色一职不串岗**：ink（图标/数字/kicker/涂鸦）、swash（只有那一笔笔刷）、accent（只有状态与高亮）、organic（只有底角色块与植物线）、grey（只有占位条/细线/描边）。
4. **底部两角有机波浪色块** + 细手绘植物线，高度 <18%，永远在最底层。
5. **上方两角小网点方阵**，各一簇。
6. **右三分之一两张异构白卡 + 一枚跨角 chip**：一张图形卡（图表/树/网格/折线），一张行列卡（列表/标签/状态行）。两张同构即失败。
7. **左三分之一 kicker + 两行超大黑体标题**，是画面第一主导元素，大幅领先。
8. **手绘涂鸦 ≤6 笔**，非对称，只落三处：chip 附近、标题与人物之间、swash 上方。

第 1 条最容易丢：模型默认会把全图统一成一种质感。第 3 条其次——色板一旦串岗，画面立刻变成随机彩色拼贴。

## 物件准确性协议（本 skill 的承重段）

这套风格的失败几乎全部集中在笔记本和手上。**六条每次必带，不可精简**，完整条款见 `references/compile-contract.md`。

1. **单体连续**：笔记本是一个完整连续的物件——键盘底座与上盖由铰链相连、共享同一透视；屏幕 UI 画在**边框内部**、四周留均匀内边距，不是浮在空中的独立面板。
2. **接触面必须点名**：桌面要在 Scene 里作为实体存在；笔记本底座整块压在桌面上，带一道紧贴的接触阴影。凡是画面里出现的物件，都要写清它落在什么面上。
3. **裁切唯一**：只有桌面被下边缘裁切。笔记本整机在画内，底边与画面下缘之间留出空隙。**绝不写"laptop fully visible"同时又写"laptop cropped by the bottom edge"**。
4. **浅角度**：笔记本走浅角、屏幕近乎正对镜头。深三分之四角 × 扁平 UI 屏幕是已知最易崩的组合。
5. **手部预算**：一只手做动作，另一只手平放或出画。不碰脸、不扶眼镜、不双手包握小物、不做手臂勾椅背或环抱这类自遮挡姿势。
6. **凝视有落点**：视线必须落在画内已存在的东西上（屏幕 / 右侧卡片 / 手上的纸）。写"看向画外某物"会让模型凭空造一个物件出来。

## 版面预算（横向加总 100%）

| 带 | 占比 | 内容 |
|---|---|---|
| 左边距 | 4% | 空 |
| 标题块 | 32% | kicker + 两行大标题，左对齐垂直居中 |
| 呼吸 | 8% | 空 |
| 人物 + 笔记本 | 24% | 头部接近上边缘 |
| 呼吸 | 3% | 空 |
| 卡片列 | 26% | 两卡 + chip，上方留背景余量 |
| 右边距 | 3% | 空 |

人物**不要超过 24%**。原始参考稿写的 32% 会把留白全部吃掉，与"generous negative space"自相矛盾。

## Read

- 编译提示词：读 `references/compile-contract.md`（铁律 + 锁死基座 + 成品骨架 + 物件条款全文）。
- 选配色 / 卡片 / 屏幕 / 人物姿态：读 `references/module-library.md`（五个配色寄存器 + 四张模块表 + 主题映射）。
- 要成品参考、或看原始五条稿的审计与修订对照：读 `references/examples.md`。

## Input

从用户输入推断，不要求填表：

1. **主题**：决定 kicker、headline、卡片类型、屏幕内容和人物职业。
2. **标题**：EN kicker ≤3 词，headline 两行合计 ≤5 词。用户原文照抄，不润色。
3. **配色寄存器**：用户没指定就按主题气质选，成组产出时逐张换。
4. **卡片标签**：三个，各 1–2 词。

## Workflow

1. **定主题落点**：这张图讲的是哪一个动作（写 / 查 / 排 / 读数 / 连）——动作决定人物姿态、屏幕内容和两张卡的类型。
2. **抽模块**：从 `module-library.md` 的四张表各抽一项——配色寄存器 / 人物调度 / 卡片组合 / 屏幕内容。**成组产出时先把整组抽取结果列成表，确认无重复再编译。**
3. **查异构**：两张卡必须一图形一行列，不同类型。同构就重抽。
4. **过物件协议**：姿态是否在手部预算内、视线是否有画内落点、笔记本是否只有一个且落在桌面上。
5. **编译**：按 `compile-contract.md` 的固定骨架输出成品，无占位符、无菜单、正文零中文（渲染标题除外）。
6. **输出**：寄存器名 + 一条完整提示词 +（可选）一句模块选型理由。用户只要 prompt 时省略解释。

**成组产出时**：Key details / Composition / Constraints 三段基座必须逐字写进**每一条**提示词——模型只看得见单条，共享规则不会自动继承。只有 Scene 的色彩变量、Subject 的情节、卡片和文字变。

## Quality Gates

出图后逐条核：

- **笔记本完整性**：一台、连续、屏幕在框内、底座压在桌上、整机未被裁切、未插进人体。这是本风格的第一失败点。
- **手**：指头数对、无第三只手、无手指穿过桌面或机身、无手碰脸。
- **网点作用域**：屏幕、卡片、桌面、背景上不得有任何网点或颗粒。
- **网点通透**：人物面部特征清晰、中间调打开，没有糊成一团黑，也没有变成一整片均匀灰。
- **描边归属**：白色贴纸边只在人物轮廓上。
- **两卡异构**：结构不同、内容不同。相同即重抽。
- **色板归位**：五个角色色各归其位，accent 没有跑到植物线上，中性灰全部偏暖。
- **文字**：只出现 kicker + 两行标题 + 三个卡片标签，屏幕上只有占位条、没有可读单词。
- 本 skill 只产出提示词，不编造已生成的图片结果。

## 状态与已知风险

**首图已实测**（2026-07-31，原稿第 3 条 Slate & Amber，成图见 `assets/preview.png`）。骨架来自五条外部参考稿，已按 `gpt-image-prompt-spec` 的重整操作做过审计与修订，审计清单见 `references/examples.md`。

首图验证成立：三质感分域、白描边只跟人物、笔记本单体连续且整机在画内、手部无畸形、五角色色板归位、英文标题与卡片小字全部渲染正确。

首图暴露两点，均已回写规则：**swash 用中性灰会在人物后面糊出一整片灰**（原稿 C1 的实证）；**屏幕上的单字母列头是想要的东西**，所以封堵只封可读单词，不封单字母和数字。

| 风险 | 状态 / 怎么修 |
|---|---|
| 笔记本屏幕脱离机身 / 双屏 | 原稿高危，首图未发生（模型自行走了近正面）。已写进物件协议 1、3、4。仍犯则把笔记本改成正对镜头、去掉角度描述 |
| 手部畸形 | 原稿四条超预算，首图（一手撑桌沿、一手叉腰）未发生。已立手部预算。仍犯则把动作手也改成平放桌面 |
| 全图被统一成一种质感 | 给被同化的一域补材质名词，一次只改一处 |
| 中文标题字形 | **未实测**——骨架按英文标题设计。中文走思源黑体 Heavy，并删掉 kicker 的 `all-caps`（中文无大小写）。逐字核对，笔画多的字多生成两张挑 |
| 成组图长成一个样 | 四张模块表每张重抽，不重复 |
| 卡片小字烂掉 | 先减文案总量，再把卡片标签压到 1 词 |

## Guardrails

- 风格用视觉特征描述，不指名艺术家、不复刻真实品牌 logo（Slack / Notion 等一律泛化为通用图形）。
- 人物是虚构泛型人物；要画真人须本人同意。
- 出图偏差先怀疑 Subject 槽点名不足，不要动 DNA 固定块；一轮只改一处。
- 已验证有效的提示词不为了合规去改，回退到最近的已知良好版本是合法动作。
