---
name: minimal-zine-poster
description: "把主题、句子、物件、情绪或文章摘要编译成极简 zine 编辑海报提示词：大留白陈纸背景、单一可想象的小主体、实验性衬线/打字机/等宽排版、一个高饱和色点、复印/网点/铅笔/凸版/扫描纸质感，安静的日韩独立杂志感。用户提到 zine 海报、极简杂志风、日杂封面、留白海报、安静编辑设计、minimal zine poster，或要把随笔、情绪、记忆向主题做成安静留白封面时使用。先内部选模块，再输出一条确定性提示词，不展示模块菜单。不用于满版商业海报、信息密集封面或其他设计流派。"
---

# Minimal Zine Poster

把主题、句子、物件、情绪或文章摘要，编译成**一条**可直接交给图像模型的极简 zine 编辑海报提示词。

风格钉子：**安静的日韩独立杂志感**——大面积陈纸留白、单一可想象的小主体、实验性排版、一个高饱和色点、印刷/扫描纸质感。

核心原则：**风格锁死，方案放开；默认输出已决策的成品 prompt；用户要模板时，输出中文可改字段在前、英文固定风格在后的复制即用结构。**

留白是主角，不是背景。这个 skill 的全部张力来自「空」和「一个安静的点」之间的关系，任何把画面填满、加多主体、加多颜色、加大段文字的冲动都要压住。

## Read

按任务读取：

- 生成提示词：读 `references/compile-contract.md` 和 `references/module-library.md`。
- 用户要示例或主题灵感：读 `references/examples.md`（如存在）。

## Input

优先从用户输入推断，不要求填表：

1. **输入模式**：文字主题、一句话、一个物件、一种情绪、文章摘要，或对现有图的风格评价。
2. **核心意象**：必须落到**一个**可想象的小主体或小视觉簇。抽象概念要转译成一个具体、安静、可画出来的物件（一枚硬币、半张褪色照片、一片叶子、一个窗口，而不是「成长」「连接」）。
3. **文字**：可包含中文短词、英文短词、一行存档式微文本或一行小副标题。用户指定中文时必须保留原中文，不默认改成英文；文字始终是安静的次要元素，绝不占据画面。
4. **画幅**：默认 3:5 竖版（zine 天然竖向）。用户指定其他比例时优先遵循；横版 zine、方版也允许，但仍保持大留白逻辑。

## Workflow

1. **识别输入**：判断是文字生成、单物件、情绪向、文章封面，还是风格评价。
2. **提取一个核心意象**：从用户内容里只抽**一个**可视化想法。宁可小、宁可安静，也不要塞多个元素。抽象主题先转成一个具体物件隐喻。
3. **选择模块**：从 `module-library.md` 选择布局、锚点、排版、纹理、色点和情绪。多张图时至少变化三类模块，优先变化核心意象、布局、色点色相、纹理和情绪，避免连续输出同一张海报换个物件。
4. **编译提示词**：按 `compile-contract.md` 输出。默认输出已决策的英文美术指导，不留下 `{}`、斜杠菜单、either/or 布局、未决选项。用户明确要求「复制即用模板 / reusable template / 文章提示词结构」时，输出中文输入字段在前、英文风格锚定在后的模板。
5. **输出**：已选模块 + 一条完整提示词 + 一句选型理由。用户只要 prompt 时，可以省略解释，只保留模块摘要和完整提示词。

## Routing Rules

- **一句话 / 一个词**：优先极简标本式布局，主体极小，留白到极致，排版只留一行贴边微文本。
- **情绪 / 记忆 / 随笔向**：优先褪色照片、半透明几何、纹理窗口这类柔软锚点，色点偏低饱和暖调，纹理偏复印柔和或扫描陈纸。
- **文章封面**：允许加一行小副标题（存档微文本或短句贴边），但必须明显小于主体气场，不做信息密集封面。
- **具体物件**：保留物件轮廓和最可识别的一两个细节，简化成安静的手绘/印刷标本，不做商品广告冲击力。
- **上传图重绘**：保留原主体身份和轮廓，退成陈纸上的单一 zine 标本，去掉原图的场景、光效和背景杂物。

## Output Format

默认成品 prompt：

````markdown
## 已选模块
- 输入模式：
- 核心意象：
- 布局模块：
- 锚点模块：
- 排版模块：
- 纹理模块：
- 色点模块：
- 情绪模块：

## 完整提示词
```text
...
```

## 选型理由
...
````

复制即用模板：

````markdown
```text
文章主题或摘要：
{输入文章主题、标题、摘要，或直接粘贴文章内容}

用途和比例：
{例如：文章封面，竖版 3:5}

指定主标题或微文本：
{可选；没有就写：自动判断}

指定副标题文字：
{可选；没有就写：自动判断或不加}

特殊要求：
{指定核心意象、色点颜色、不能出现的元素；没有就写：无}

Create a minimal zine editorial poster from the theme above.

Style lock:
...
```
````

模板里不要加入「你只需要改这里」「以下内容不用改」之类说明行。字段本身已经足够清楚。

## Quality Gates

- 必须是安静的日韩独立杂志 / zine 编辑逻辑，不是满版海报、商业广告或信息图。
- 陈纸留白必须是画面主角：负空间占 70%-90%，视觉簇只占 8%-25%。
- 必须只有**一个**可想象的核心意象或小视觉簇，不堆多主体。
- 必须只有**一个**高饱和色点，占画布约 0.8%-2.5%；其余画面是纸色、墨色和低饱和灰调。
- 排版必须安静次要：衬线、打字机、等宽或手写体，可以是碎片字母、贴边微文本、存档标签、幽灵文字、极简标题；绝不做粗黑大标题霸屏。
- 用户要求中文时可输出中文文字，必须使用用户指定原文；避免假中文、乱码字、随机标签。
- 副标题只能一行，明显小于核心意象气场；中文建议 6-12 字，英文 2-6 词。
- 纹理必须像真实印刷/扫描材料：复印、网点、丝网晕散、胶片颗粒、凸版压痕、陈纸斑驳、铅笔灰；不是后期脏滤镜或数字做旧。
- 成组输出时不能只换意象。每张图的核心意象、布局、纹理、色点色相或情绪至少三项明显不同。
- 禁止：满版场景、商业海报冲击力、3D/霓虹/卡通/渐变、多物体、多颜色、大段正文、现代 UI 干净矢量感、影棚打光、写实产品渲染。
- 不编造已经生成的图片结果；本 skill 只产出提示词或风格评价。
